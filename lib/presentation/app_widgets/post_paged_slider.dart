import 'package:flutter/material.dart';

class PostPagedSlider extends StatelessWidget {
  const PostPagedSlider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
