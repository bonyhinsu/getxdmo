import 'package:flutter/material.dart';

class PaymentMethodShimmer extends StatelessWidget {
  const PaymentMethodShimmer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
